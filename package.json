{
  "name": "socratic-path-prototype",
  "version": "1.1.0",
  "private": true,
  "description": "An evidence-gated algebra tutor that supports productive struggle.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "evaluate": "node scripts/run-evaluation.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
